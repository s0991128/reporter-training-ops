# 117건 원본대조 검증

이 문서는 `업무목록.csv`를 정본으로 삼아 현재 코드가 원본을 어떻게 읽고 변환하는지 확인한 결과다. 업무 문장, key, 구간 순서는 수정하지 않았으며, metadata의 `PENDING_REVIEW` 상태도 변경하지 않았다.

## 자동 검증 결과

| 항목 | 결과 |
| --- | ---: |
| CSV 업무 행 | 117건 |
| key | 117건 |
| 중복 key | 0건 |
| 필수값 누락 | 0건 |
| 구간 | 16개 |
| CSV 구간 순서 | PASS |
| 3회 체크 업무 | 10건 |
| metadata key | 117건 |
| metadata orphan / missing | 0건 / 0건 |
| 운영 task 변환 | 117건 |
| source checksum | `1e2944ee` |
| 구조적 오류 | 0건 |

검증은 `tests/contest-validation.test.js`가 CSV 파싱, 행 검증, metadata 병합, `createOperationalTasks()`까지 실제 함수로 수행한다. 후보 분류는 업무 의미를 확정하는 판정이 아니라 후속 담당자 검토를 위한 문자열·반복 패턴 탐색이다. 한 업무가 여러 후보 규칙에 걸릴 수 있으므로 후보 유형 건수는 중복 집계된다.

## 검토 후보 요약

| 후보 유형 | 건수 | 탐지 기준 |
| --- | ---: | --- |
| 일반 | 34건 | 다른 후보 규칙에 걸리지 않는 단일 문장 |
| 조건부 후보 | 13건 | `있는 경우`, `요청이 있었던 경우`, `요구한`, `누락된 경우` |
| 반복업무 | 48건 | 동일 업무 문장의 반복 |
| 개인정보 주의 | 1건 | `주민등록번호` 등 명시어 |
| 선후관계 검토 후보 | 30건 | 확정·수령·완료·전달·회수 등의 표현 |
| 예산/정산 연계 후보 | 21건 | 견적·구매·결제·정산·지출결의 등 |
| 수행시점 검토 후보 | 5건 | 기간 범위·매일·주말 표현 |

개인정보 주의 후보는 `task-2`의 주민등록번호 문장이다. 이는 개인정보를 저장하거나 처리하도록 승인한 결과가 아니다. 모든 후보의 `required`, `riskLevel`, `dependencies`는 자동 확정하지 않으며 담당자 검토 전 상태를 유지한다.
